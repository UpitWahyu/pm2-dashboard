# Port HTTP server agent
PORT=4001

# Token auth yang dipakai dashboard untuk akses agent ini.
# WAJIB diganti dengan random string kuat (contoh: openssl rand -hex 32)
AGENT_TOKEN=change-me-strong-random-token

# Nama server ini (ditampilkan di dashboard)
AGENT_NAME=vps-1
